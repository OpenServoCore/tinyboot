[package]
name = "boot"
version = "0.1.0"
edition = "2024"

[dependencies]
tinyboot-ch32 = { path = "../../..", features = ["ch32v003f4p6", "rt", "defmt", "trial-boot"] }
tinyboot = { path = "../../../../tinyboot", default-features = false, features = ["defmt", "trial-boot"] }
panic-halt = "1.0"

defmt = "1.0"
defmt-rtt = "1.1"
